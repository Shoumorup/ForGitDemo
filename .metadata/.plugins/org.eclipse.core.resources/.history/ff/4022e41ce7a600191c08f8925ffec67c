package com.visa.training.jdbc;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.util.List;

public class ProductDAO {
	
	public void save(Product toBeSaved) throws Exception{
		Connection c = JdbcUtil.getConnection();
		PreparedStatement stmt = c.prepareStatement("INSERT into `product`(product_id,product_name,product_price,product_qoh) VALUES (?,?,?,?)");
		stmt.setInt(1,toBeSaved.id);
		stmt.setString(2, toBeSaved.name);
		stmt.setFloat(3, toBeSaved.price);
		stmt.setInt(4, toBeSaved.qoh);
		stmt.executeUpdate();
	}
	
	public Product findById(int id)
	{
		//Read based on ID
	}
	
	public List<Product> findAll()
	{
		//Read all
	}
	
	public void remove(int id)
	{
		
	}
	
	public void update(Product updated)
	{
		
	}
}
